# Drafts (parked — out of the master's volume)

Files here are excluded from the Quarto render (the leading `_` makes Quarto
ignore the directory). They are out of scope for the master's book (see
`../CLAUDE.md`) and are kept for reference / possible Volume 2 seeding:

- `comp.qmd`, `multisynth.qmd`, `Proofbound.qmd` — draft chapters not in the book.
- `seasonality_synth.ipynb`, `syn_data.ipynb` — experimental notebooks.
- `multiplesynth.py`, `multiplesynthhelpers.py`, `spotify_proximal_helpers.py`,
  `sparsedense.py` — helper modules no in-scope chapter imports.
