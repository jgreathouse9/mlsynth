"""Reference implementations for cross-validation benchmarks.

Reference code that ships under a permissive licence (or as an installable
package) is consumed directly. Code that is *not* redistributable -- e.g. an
authors' replication repo with no licence -- is **cloned on demand** at a pinned
commit rather than vendored into this tree. See ``clone_spsydid``.
"""
