"""GeoLift market selection.

Nominates candidate test-market sets and scores them. ``helpers`` holds the
treatment-agnostic, estimator-independent leaves (similarity ranking, candidate
generation) that the selection pipeline composes.
"""
