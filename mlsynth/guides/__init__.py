"""Bundled agent/LLM guides for mlsynth (llms.txt and variants).

These plain-text files are the machine-readable counterpart to the docs: a
concise, always-current index an LLM (or a user via an LLM) can read to pick
and call the right estimator. Regenerate with ``python tools/gen_llms_txt.py``.
"""
